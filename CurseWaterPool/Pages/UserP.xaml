<Page x:Class="CurseWaterPool.Pages.UserP"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CurseWaterPool.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UserP">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="600"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Name="TolBarGrid">
            <Grid x:Name="GridButtonsBar" >
                <StackPanel>
                    <Button x:Name="ProfileBT" Content="Профиль" Margin="10,5" Click="ProfileBT_Click"/>
                    <Button x:Name="SubBT" Content="К абонементам" Margin="10,5" Click="SubBT_Click" />
                </StackPanel>
                <StackPanel VerticalAlignment="Bottom">
                    <Button x:Name="BackBT" Content="Выйти" Margin="10,5" Click="BackBT_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
        <Grid Grid.Column="1">
            <Image Width="200" Source="/Source/WaterPoolLogo.jpg" />
        </Grid>
    </Grid>
</Page>
